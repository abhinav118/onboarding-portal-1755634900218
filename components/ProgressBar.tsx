interface ProgressBarProps {
  animated?: any
  showLabel?: any
}

export default function ProgressBar({ animated, showLabel }: ProgressBarProps) {
  return (
    <div className="">
      {/* ProgressBar component content */}
    </div>
  )
}